The repository contains separate implementations for each CPU scheduling algorithm:
FCFS.cpp
SJF.cpp
SRTF.cpp
RR.cpp
Priority_Scheduling_NonP.cpp

Each file can be compiled and executed individually.
OR 
The file code_combined.cpp contains all scheduling algorithms combined in a single program and can be used to run and compare all algorithms at once using the same test case.
